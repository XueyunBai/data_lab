# In‑Memory Data Structures as Databases (Starter)

Run each part:

```bash
python3 part1_lists_as_table.py
python3 part2_dicts_as_rows.py
python3 part3_tuples_as_records.py
python3 part4_index_by_id.py
```

If you finish early, do:

```bash
python3 challenge_extension.py
```
